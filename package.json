{
  "name": "ngrx-store-hmr",
  "version": "0.0.3",
  "description": "@ngrx/store middleware to connect store state to the Angular2-HMR API",
  "main": "index.js",
  "scripts": {
    "pretest": "rm -rf ./tmp && tsc -p spec",
    "test": "jasmine",
    "prebuild": "rm -rf ./dist",
    "build": "tsc",
    "postbuild": "cp ./{readme.md,package.json} ./dist"
  },
  "author": "Mike Ryan",
  "license": "MIT",
  "peerDependencies": {
    "@angular/core": "2.0.0-rc.2",
    "@ngrx/core": "^1.0.0",
    "@ngrx/store": "^2.0.0",
    "rxjs": "5.0.0-beta.6"
  },
  "devDependencies": {
    "typescript": "^1.8.9"
  }
}
